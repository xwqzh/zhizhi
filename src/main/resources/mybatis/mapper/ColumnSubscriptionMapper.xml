<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.xu.repository.mapper.ColumnSubscriptionMapper">

    <resultMap id="BaseResultMap" type="cn.xu.model.entity.ColumnSubscription">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="column_id" property="columnId"/>
        <result column="status" property="status"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <resultMap id="ColumnResultMap" type="cn.xu.model.entity.Column">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="cover_url" property="coverUrl"/>
        <result column="status" property="status"/>
        <result column="post_count" property="postCount"/>
        <result column="subscribe_count" property="subscribeCount"/>
        <result column="is_recommended" property="isRecommended"/>
        <result column="sort" property="sort"/>
        <result column="last_post_time" property="lastPostTime"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, column_id, status, create_time, update_time
    </sql>

    <insert id="insert" parameterType="cn.xu.model.entity.ColumnSubscription" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO column_subscription (user_id, column_id, status, create_time, update_time)
        VALUES (#{userId}, #{columnId}, #{status}, #{createTime}, #{updateTime})
    </insert>

    <update id="update" parameterType="cn.xu.model.entity.ColumnSubscription">
        UPDATE column_subscription
        SET status = #{status}, update_time = NOW()
        WHERE id = #{id}
    </update>

    <select id="selectByUserAndColumn" resultMap="BaseResultMap">
        SELECT <include refid="Base_Column_List"/>
        FROM column_subscription
        WHERE user_id = #{userId} AND column_id = #{columnId}
    </select>

    <select id="selectSubscribedColumns" resultMap="ColumnResultMap">
        SELECT c.id, c.user_id, c.name, c.description, c.cover_url, c.status, c.post_count, 
               c.subscribe_count, c.is_recommended, c.sort, c.last_post_time, c.create_time, c.update_time
        FROM `column` c
        INNER JOIN column_subscription cs ON c.id = cs.column_id
        WHERE cs.user_id = #{userId} AND cs.status = 1 AND c.status = 1
        ORDER BY cs.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countSubscribedColumns" resultType="int">
        SELECT COUNT(*)
        FROM column_subscription cs
        INNER JOIN `column` c ON cs.column_id = c.id
        WHERE cs.user_id = #{userId} AND cs.status = 1 AND c.status = 1
    </select>

    <select id="isSubscribed" resultType="int">
        SELECT COUNT(*)
        FROM column_subscription
        WHERE user_id = #{userId} AND column_id = #{columnId} AND status = 1
    </select>

    <select id="batchCheckSubscribed" resultType="long">
        SELECT column_id
        FROM column_subscription
        WHERE user_id = #{userId} AND status = 1
        <if test="columnIds != null and columnIds.size() > 0">
            AND column_id IN
            <foreach collection="columnIds" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </select>

    <select id="selectSubscribedColumnIds" resultType="long">
        SELECT cs.column_id
        FROM column_subscription cs
        INNER JOIN `column` c ON cs.column_id = c.id
        WHERE cs.user_id = #{userId} AND cs.status = 1 AND c.status = 1
        ORDER BY cs.create_time DESC
        LIMIT #{offset}, #{limit}
    </select>

    <select id="countDailySubscriptions" resultType="map">
        SELECT DATE(create_time) AS statDate, COUNT(*) AS statCount
        FROM column_subscription
        WHERE column_id = #{columnId}
          AND create_time &gt;= #{startTime}
          AND create_time &lt; #{endTime}
        GROUP BY DATE(create_time)
        ORDER BY statDate ASC
    </select>

</mapper>
